import React from "react";

import Nav from ".";

export default {
  title: "Sections/Nav",
  component: Nav,
};

const Template = (args) => <Nav {...args} />;

export const Default = Template.bind({});
